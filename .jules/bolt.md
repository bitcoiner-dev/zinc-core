## 2026-04-10 - Optimize Hash Map Initialization\n**Learning:** Initializing a HashMap without capacity when pushing elements inside a loop can lead to multiple dynamic memory allocations and expensive rehashing when handling collections like inscriptions. \n**Action:** Use `HashMap::with_capacity(len)` where the upper-bound size of incoming data is known, to eliminate intermediate heap reallocations and improve execution speed.
